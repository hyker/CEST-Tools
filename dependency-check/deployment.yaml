apiVersion: apps/v1
kind: Deployment
metadata:
  name: dependency-check-enclave
  labels:
    app: dependency-check-enclave
spec:
  selector:
    matchLabels:
      app: dependency-check-enclave
  template:
    metadata:
        labels:
          app: dependency-check-enclave
    spec:
      volumes:
        - name: pod-storage
          persistentVolumeClaim:
            claimName: storage-claim
        - name: var-run-aesmd
          hostPath:
            path: /var/run/aesmd
      tolerations:
      - key: kubernetes.azure.com/sgx_epc_mem_in_MiB
        operator: Exists
        effect: NoSchedule
      containers:
      - name: dependency-check-enclave
        image: hyker/dependency-check-enclave
        imagePullPolicy: Always
        env:
        - name: SGX_AESM_ADDR
          value: "1"
        volumeMounts:
          - mountPath: "/pod-storage/"
            name: pod-storage
          - name: var-run-aesmd
            mountPath: /var/run/aesmd
        resources:
          limits:
            kubernetes.azure.com/sgx_epc_mem_in_MiB: 10